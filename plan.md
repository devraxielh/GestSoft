1. Add a "Terminar documento" button next to "Guardar" and "Generar PDF" in `src/app/dashboard/documentos/[id]/page.tsx`.
   - The button should change the status of the document to "Terminado".
   - If the document is already "Terminado", the button should say "Reabrir documento" and change the status back to "En construcción".
2. Add a `handleToggleStatus` function to handle the API call to update the status.
3. Pre-commit instructions.
4. Submit
